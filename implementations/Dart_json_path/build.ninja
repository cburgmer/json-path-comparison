root = implementations/Dart_json_path
builddir = $root/build

rule install
  command = (cd $root && PUB_CACHE=build/pub-cache dart pub get)

build $builddir/pub-cache: install | $root/pubspec.yaml $root/pubspec.lock

build $root/install: phony $builddir/pub-cache
