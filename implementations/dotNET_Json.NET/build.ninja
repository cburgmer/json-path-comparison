root = implementations/dotNET_Json.NET
builddir = $root/build

rule build
  command = cd $root && dotnet build -o $$(basename $out)

build $builddir: build | $root/Dotnet_Json.NET.csproj $root/Program.cs

build $root/install: phony $builddir
