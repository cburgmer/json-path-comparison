root = implementations/JavaScript_swaggerexpert-jsonpath
builddir = $root/build

rule install
  command = (cd $root && npm install) && echo > $out

build $builddir/node_modules_installed: install | $root/package.json

build $root/install: phony $builddir/node_modules_installed
